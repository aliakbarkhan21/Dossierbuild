# Dossierbuild dependencies
# Phase 1: master profile schema, storage, editor, resume import
streamlit>=1.40
pydantic>=2.7

# Resume import (phase 1b) -- text extraction from an existing resume
pypdf>=4.2
python-docx>=1.1

# AI parsing / tailoring (phase 1b import parse, phase 3 tailoring)
google-genai>=1.0
python-dotenv>=1.0

# PDF generation (phase 2)
jinja2>=3.1
playwright>=1.44

# Image handling: the brand mark, the favicon, and the resume portrait.
# Streamlit pulls this in anyway; declared because we import it directly.
pillow>=10.0
